import React from "react";
import styled from "styled-components/macro";

const PageHeading = styled.h2`
  font-size: 2rem;
`;

const History = () => {
  return <PageHeading>History</PageHeading>;
};

export default History;
